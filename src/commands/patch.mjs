import Command from '../base/commands.mjs';

const patch = new Command("patch", 10, "info", "Shows all patch information.", (tempMessage, tempArgs, tempPrefix) => {
  
  if(!tempArgs){
    
  }
});

export default patch;
